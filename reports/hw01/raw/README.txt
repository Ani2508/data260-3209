This folder holds machine-readable per-run outputs.
After Part 3 runs, it will contain:
  - nondeterminism_runs.csv (or .json): tags + latency for all 40 runs
  - token_counts.json: per-turn token accounting (Part 4), if exported
